filetype: xml

detect: 
    filename: "\\.(xml|sgml?|rng|plist)$"

rules:
    - symbol.tag: "<.*?>"
    - preproc:
        start: "<!DOCTYPE"
        end: "[/]?>"
        rules: []

    - comment: "<!--.+?-->"
    - special: "&[^;]*;"
